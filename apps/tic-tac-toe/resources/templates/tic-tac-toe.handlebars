{{#view "TicTacToe.gameView"}}
  <div id="whoseTurnIsIt"> {{whoseTurnIsIt}} </div>
  {{#collection "TicTacToe.gameBoardView"}}
    {{#view "TicTacToe.CellView"}}
      <div class="markerBox {{positionalClasses}} {{playerClass}}" style="width: 100px; height: 100px; top: {{top}}px; left: {{left}}px">
        {{mark}}
      </div>
    {{/view}}
  {{/collection}}
{{/view}}
